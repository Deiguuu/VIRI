# Visión por computadora y captura
mediapipe==0.10.0
opencv-python==4.7.0.72

# Control de mouse y teclado
pyautogui==0.9.53
keyboard==0.13.5
mouse==0.7.1

# Reconocimiento y síntesis de voz
SpeechRecognition==3.8.1
pyttsx3==2.90

# Reconocimiento de voz avanzado (opcional)
vosk==0.3.45
openai==0.27.8

# Transcripción por Whisper (requiere ffmpeg)
whisper==1.1.5

# Servidor API con WebSockets (elige uno según prefieras)
fastapi==0.100.0
uvicorn==0.23.2

# Alternativa Flask + Socket.IO (descomenta si usas)
# flask==2.3.2
# flask-socketio==5.3.4

# Utilidades generales
numpy==1.24.3

# Sistema y procesamiento
json5==0.9.12
python-multipart==0.0.6  # Para manejar archivos multipart en FastAPI si subes archivos
